<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Best Chicago Bars</title>
</head>
<body>
    <nav> Log in  Sign up <a href="bars/new">New Bar</a></nav>
    <h1>Chicago bar finder</h1>
    

    <div class="wrapper">
        <% for (let i = 0; i < Bars.length; i++) { %>
        <div class="barcontainer">
            <div class="title">
                <a href="/bars/<%=Bars[i].id%>"><%= Bars[i].name %></a>   
            </div>
            <div class="barImage">
                <a href="/bars/<%=Bars[i].id%>"><img src="<%= Bars[i].img %>"></img></a>
            </div> 
            <p class="location"><%=Bars[i].location%></p>
        </div>
        <% } %>
    </div>
</body>
</html>